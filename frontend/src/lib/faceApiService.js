// Re-export from api.js for backward compatibility.
// The canonical implementation is now in api.js (enrollEmployeeFacesBatch).
export { enrollEmployeeFacesBatch as registerEmployeeFaceIdentity } from './api'